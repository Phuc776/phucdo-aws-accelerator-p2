apiVersion: v1
kind: Service
metadata:
  name: observability-demo
  labels:
    app: observability-demo
spec:
  selector:
    app: observability-demo
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP